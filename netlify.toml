[build]
  # No build command needed for static HTML
  command = "echo 'No build required'"
  
  # Publish directory is the root (where index.html is)
  publish = "."

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
